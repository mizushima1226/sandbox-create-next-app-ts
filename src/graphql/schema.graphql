type Query {
    users: [User!]!
    allTasks: [Task!]!
}

type User {
    name: String
    age: Int
}

type Task {
    id: ID!
    title: String!
    description: String
}

schema{
    query: Query
}